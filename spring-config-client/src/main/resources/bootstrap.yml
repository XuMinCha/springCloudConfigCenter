server:  port: 8083  servlet:    context-path: /clientspring: 
  cloud:
    config:
      name: application
      profile: dev
      label: master
      #config server uri
      uri: http://localhost:50001        